
<body>
<div>
</div>
<h1>Books</h1> 
<table>
 <thead>
        <tl>
            <th>Title</th>
            <th>body</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <% @books.each do |book| %>
    <tbody>
         <td><%= book.title %></td>
         <td><%= book.body %></td>
         <td><%= link_to"Show", "/books/#{book.id}" %></td>
         <td><%= link_to"Edit", "/books/#{book.id}/edit" %></td>
         <td><%= link_to"Destroy", "/books/#{book.id}",method: :delete,"data-confirm"=>"are you sure?" %></td>
        </tr>
      </tody>
     <% end %>
    </table>
      

<h2>New  book</h2>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being
  <ul>
    <% @book.errors.full_messages.each do |message| %>
     <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
   
 <%= form_with model: @book, url: '/books', method: :post do |f| %> 
 
 <div calss="field">
  <tabel for="book_title">Title</tabel>
     <%= f.text_field :title %>
 </div>
 
  <div class="field">
   <tabel for="book_body">Body</tabel>
      <%= f.text_area :body %> 
  </div>
  
<div class="actions">
 <%= f.submit 'Create Book' %>
 </div>
 
<% end %>

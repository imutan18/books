<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being
  <ul>
    <% @book.errors.full_messages.each do |message| %>
     <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @book, url: update_book_path(@book), method: :patch do |f| %> 


<div>
<h4>Title</h4> 
<%= f.text_field :title %> 
</div>


<div>
<h4>Body</h4>
<%=f.text_area :body %>
</div>

<div>
 <%= flash[:notice] %>
<%= f.submit 'Update Book' %> 
</div>

 <tbody>
         <td><%= link_to"Show", "/books/#{@book.id}" %></td>
           |
         <td><%= link_to"back", "/books" %></td>
      </tody>
     <% end %>